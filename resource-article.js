(()=>{const tocHeading=document.querySelector('.article-toc>p');if(tocHeading)tocHeading.textContent='Contents';const bar=document.querySelector('.article-progress');const links=[...document.querySelectorAll('.article-toc a[href^="#"]')];const sections=links.map(link=>document.querySelector(link.getAttribute('href'))).filter(Boolean);const updateProgress=()=>{if(!bar)return;const doc=document.documentElement;const max=doc.scrollHeight-doc.clientHeight;const pct=max>0?Math.min(100,Math.max(0,(doc.scrollTop/max)*100)):0;bar.style.width=`${pct}%`};const updateActive=()=>{if(!links.length||!sections.length)return;let active=sections[0].id;for(const section of sections){if(section.getBoundingClientRect().top<=180)active=section.id}links.forEach(link=>link.classList.toggle('is-active',link.getAttribute('href')===`#${active}`))};let ticking=false;const onScroll=()=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{updateProgress();updateActive();ticking=false})};addEventListener('scroll',onScroll,{passive:true});addEventListener('resize',onScroll,{passive:true});links.forEach(link=>link.addEventListener('click',()=>{links.forEach(item=>item.classList.remove('is-active'));link.classList.add('is-active')}));updateProgress();updateActive()})();